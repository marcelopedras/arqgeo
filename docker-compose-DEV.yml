version: "3"
services:
  postgres:
    build: ./postgres
    hostname: postgres
    #restart: always
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  db_data:
